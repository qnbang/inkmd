# inkmd 기능 설명서

가장 가벼운 맥용 마크다운 편집기. 이 문서를 **미리보기(⇧⌘P)**로 열면 아래 기능이 전부 실제로 렌더됩니다.

## 제목 위계

문서 구조는 제목·부제목·소제목으로 잡습니다.

### 소제목입니다

본문은 이렇게 이어집니다. 편집 화면에서는 `#` 기호가 보이지 않고 크기로만 구분됩니다.

## 글자 서식

**굵게**, *기울임*, 그리고 `인라인 코드`를 문장 안에서 바로 씁니다.
단축키는 굵게 ⌘B, 기울임 ⌘I.

## 목록

순서 없는 목록:

- 첫 번째 항목
- 두 번째 항목
- 세 번째 항목

순서 있는 목록 (숫자만 치고 스페이스 → 자동 시작):

1. 하나
2. 둘
3. 셋

## 표 — 미리보기에서 칸을 눌러 바로 편집

미리보기 상태에서 아래 표의 칸을 클릭하면 그 자리에서 값을 고칠 수 있고, 원문 `.md`에 자동 저장됩니다.

| 기능 | 단축키 | 지원 |
|:-----|:------:|-----:|
| 저장 | ⌘S | O |
| 미리보기 | ⇧⌘P | O |
| 찾기 | ⌘F | O |
| 실행취소 | ⌘Z | O |

## 인용과 구분선

> 인용문은 이렇게 표시됩니다. 메모나 강조에 씁니다.

---

## 코드 블록

```
swift build -c release
bash 앱만들기.sh
```

## 링크와 이미지

[깃허브 저장소](https://github.com/qnbang/inkmd) 처럼 링크를 걸고,
아래처럼 이미지도 문서 안에서 렌더됩니다.

![inkmd 미리보기 화면](docs/screenshot.png)

## 그 밖에

- 폴더 트리에는 폴더와 `.md` 파일만 보입니다 (png 등은 숨김, 문서 안에서는 렌더됨)
- 다른 파일로 넘어가면 자동 저장 (아이폰 메모 방식)
- 파일명은 더블클릭 또는 엔터로 그 자리에서 변경, 우클릭으로 삭제(휴지통)
- 파일은 순수 `.md` 텍스트라 git·다른 도구와 그대로 호환
